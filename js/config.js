
const CONFIG = {
    APP_NAME: "EcoConnect",
    
    // When you move to backend API later:
    API_BASE: "",   // e.g. "/api"

    STORAGE_MODE: "local", 
    // local = localStorage (demo)
    // api = backend calls

    VERSION: "2.0-pro"
};
